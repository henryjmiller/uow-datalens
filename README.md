## DataLens

A web-based data analysis tool built with Next.js. Upload CSV datasets, profile the data, run analyses, visualise results, and build predictive models.

### Running locally

```
npm install
npm run dev
```

Requires a `.env.local` file with a `COSMOS_CONNECTION_STRING` value pointing to an Azure Cosmos DB account.
